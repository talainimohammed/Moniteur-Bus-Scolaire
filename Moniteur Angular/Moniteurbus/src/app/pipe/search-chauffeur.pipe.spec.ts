import { SearchChauffeurPipe } from './search-chauffeur.pipe';

describe('SearchChauffeurPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchChauffeurPipe();
    expect(pipe).toBeTruthy();
  });
});
