import { SearchBusPipe } from './search-bus.pipe';

describe('SearchBusPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBusPipe();
    expect(pipe).toBeTruthy();
  });
});
