import { SearchetudiantPipe } from './searchetudiant.pipe';

describe('SearchetudiantPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchetudiantPipe();
    expect(pipe).toBeTruthy();
  });
});
